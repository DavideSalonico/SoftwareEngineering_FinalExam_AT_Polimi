package GC_11.View;

import GC_11.util.Observer;
import java.util.Observable;

public class View extends Observable implements Observer {

    @Override
    public void update(GC_11.util.Observable o, Enum arg) {

    }
}
